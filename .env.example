# FocusFlow is local-first and requires no environment variables.
# All data is stored in your browser (localStorage + IndexedDB).
# Copy this file to .env only if you ever need to add Vite-prefixed
# configuration (e.g. VITE_* variables) — never commit real values.
